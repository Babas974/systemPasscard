# Changelog — 2026-06-05

---

## Backend Rust (dashboard-app/app/src-tauri/)

- `src/db.rs`
  - Table `logs` ajoutee : `id`, `source`, `niveau`, `message`, `date_heure`
  - Index : `idx_logs_date`, `idx_logs_niveau`
  - Fonctions : `inserer_log`, `lister_logs`, `compter_logs`, `supprimer_tous_logs`, `supprimer_logs_anciens`
  - Type `LogEntry` (Serialize)
  - Type `LogEmitter = Arc<dyn Fn(...) + Send + Sync>` + `noop_log_emitter()`
  - Fonction utilitaire `date_heure_maintenant_ms()` (avec millisecondes)
- `src/routes.rs`
  - `LogRequest` (Deserialize) : `source`, `niveau?`, `message`
  - `LogsQuery` (Deserialize) : `limit?`, `niveau?`
  - Handler `POST /debug/log`
  - Handler `GET /debug/logs` (filtrable par niveau, limite 1..5000)
  - Handler `DELETE /debug/logs`
  - `HttpState` enrichi du champ `log_emitter: db::LogEmitter`
- `src/main.rs`
  - `build_log_emitter(app: AppHandle)` — emet `nouveau-log` via Tauri
  - 3 routes HTTP ajoutees dans le `HttpServer::new`

---

## Front Android (appCollege/)

- `Logger.ts` (nouveau)
  - File d'attente interne (max 200)
  - `log()`, `logInfo()`, `logWarn()`, `logError()`, `logFatal()`
  - `startLogFlusher(intervalMs)` / `stopLogFlusher()`
  - `installGlobalErrorHandler(source)` — wrap `ErrorUtils.setGlobalHandler`
  - Envoi via `POST /debug/log` (timeout 2.5s, abort controller)
- `StorageService.ts`
  - Wrapper resistant : `isAsyncStorageUsable()` + try/catch sur chaque appel
  - Fallback memoire (`inMemoryFallback`) si AsyncStorage natif absent
  - Log automatique des erreurs via Logger
- `ApiService.ts`
  - Export ajoute : `getApiBaseUrl()`
- `App.tsx`
  - Import et init au demarrage : `startLogFlusher(3000)`, `installGlobalErrorHandler('App')`
  - `useEffect` d'init wrappe dans try/catch avec `logInfo` + `logFatal`

---

## Front Desktop (dashboard-app/app/src/)

- `DebugPanel.tsx` (nouveau)
  - Polling `GET /debug/logs` toutes les 5s
  - Ecoute event Tauri `nouveau-log` (push temps-reel)
  - Filtres par niveau : tous / debug / info / warn / error / fatal
  - Recherche full-text (message, source, niveau)
  - Auto-scroll en haut, vidage, rechargement manuel
  - Badge de statut connecte/deconnecte
  - Couleurs par niveau (debug gris, info bleu, warn orange, error rouge, fatal rose)
- `App.tsx`
  - Import `DebugPanel`
  - Toggle existant `debugOuvert` branche sur le nouveau composant

---

## Build & verification

- `cargo build --release` (dashboard-app) : OK, 1 warning (import `Serialize` unused dans routes.rs)
- `pnpm tauri build` (dashboard-app) : OK
- Bundle : `dashboard-app/app/src-tauri/target/release/bundle/rpm/pont-saisie-desktop-0.2.1-1.x86_64.rpm`
- Test endpoint : `POST /debug/log` repond `{"id":1,"statut":"ok"}`, `GET /debug/logs` retourne le log

---

## AVERTISSEMENT — Regle n°5 enfreinte

Les modifications ci-dessus ont ete effectuees **AVANT** la creation de ce fichier de suivi, ce qui contrevient a la regle de validation prealable. Voir `.ai/ai-todo/todo-2026-06-05.md` pour le rattrapage.
