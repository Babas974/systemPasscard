# TODO — 2026-06-05

## Fait (a valider / completer si besoin)

- [x] Backend Rust : table `logs` SQLite + handlers `POST /debug/log`, `GET /debug/logs`, `DELETE /debug/logs` (db.rs, routes.rs, main.rs)
- [x] Backend Rust : emission temps-reel des logs via Tauri `emit("nouveau-log", ...)`
- [x] Front Android : `Logger.ts` (file d'attente + flush periodique via POST)
- [x] Front Android : `StorageService.ts` resistant (try/catch + fallback memoire si AsyncStorage natif absent)
- [x] Front Android : `App.tsx` — init wrappe dans try/catch avec `logInfo` / `logFatal`
- [x] Front Desktop : `DebugPanel.tsx` — panneau dedie, filtres par niveau, recherche, temps-reel via event Tauri
- [x] Front Desktop : `App.tsx` — DebugPanel branche sur le toggle existant `debugOuvert`
- [x] Build : `cargo build --release` et `pnpm tauri build` OK (warning import unused non bloquant)

## En cours / bloque

- [x] Tablette reconnectee
- [x] Metro + desktop relances
- [x] App lancee sur tablette : log `Demarrage v0.0.2` emis correctement par notre logger
- [x] **Diagnostic** : 0/4 logs emis par l'app n'arrivent au PC
  - Cause 1 : flusher periodique (3s) trop lent vs crash (0.2s)
  - Cause 2 : `import AsyncStorage` au top-level fait crasher tout le module StorageService
- [ ] **FIX 1** : StorageService en `require` dynamique + try/catch (evite crash au load)
- [ ] **FIX 2** : flush immediat pour FATAL/ERROR (fetch synchrone, ne depend pas du flusher)
- [ ] **FIX 3** : lock sur la queue (evite double envoi)
- [ ] Rebuild APK (sans Metro) pour fonctionnement **offline** (sans internet, ni PC)

## Prochaine etape proposee (A VALIDER)

1. Reconnecter tablette
2. Relancer Metro + tunnel ADB
3. Lancer l'app Android : envoyer un scan de test
4. Verifier que :
   - L'app Android demarre (StorageService resistant)
   - Le log "Demarrage v..." apparait dans DebugPanel desktop
   - Le scan apparait dans le tableau desktop

## Nouvelles actions (validees 2026-06-05 fin de journee)

### Suppressions (demande user : simplification usage)
- [ ] Supprimer `Classe` type + `CLASSES` array + radio buttons dans App.tsx
- [ ] Supprimer scanner QR + bouton "Scanner QR" dans App.tsx + import QRScannerScreen
- [ ] Supprimer modal "Configurer l'IP du PC" legacy dans App.tsx + bouton "PC : ... (Connecte/Deconnecte)"
- [ ] Supprimer bloc "IP du PC" + bouton "Enregistrer l'IP" dans SettingsScreen.tsx
- [ ] Retirer permission CAMERA du AndroidManifest.xml (scanner QR supprime)
- [ ] Retirer ACCESS_WIFI_STATE du AndroidManifest.xml (non utilise)
- [ ] Garder IP par defaut `127.0.0.1` en dur (tunnel ADB USB reverse)
- [ ] IP alternative `10.154.202.45` documentee en commentaire pour mode Wi-Fi partage

### Permissions runtime
- Plus de permission "dangerous" requise (CAMERA retiree)
- INTERNET / VIBRATE / ACCESS_NETWORK_STATE = "normal" permissions, accordees a l'install
- Aucune demande runtime necessaire

### Build & test
- [ ] Rebuild APK : `cd appCollege/android && ./gradlew assembleDebug` (5-10 min, inclut autolinking AsyncStorage)
- [ ] Verifier tunnels ADB actifs : `adb reverse --list` (8080, 8081)
- [ ] Push APK : `adb install -r app-debug.apk`
- [ ] Lancer app, envoyer un scan, verifier apparition dans tableau desktop

## Questions ouvertes

- Le `pkill` que j'ai fait a-t-il ete percu comme agressif ? Souhaites-tu que je privilegie `kill` (SIGTERM) et demande confirmation avant ?
- Le panneau debug est-il dans le bon endroit (drawer lateral droit) ou prefere-t-tu un onglet integre a l'app ?
