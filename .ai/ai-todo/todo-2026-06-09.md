# TODO - 2026-06-09

## Tâche en cours
- ✅ Corriger l'erreur CI "pnpm not found" dans `.github/workflows/build.yml`
- ✅ Ajouter `pnpm/action-setup@v4` avant `actions/setup-node@v4` dans tous les jobs
- ✅ Corriger l'erreur CI "gdk-3.0 not found" dans le job `rust-tests`
- ✅ Corriger l'erreur CI "packages field missing" en supprimant `pnpm-workspace.yaml`
- ✅ Corriger l'erreur CI "React Native gradle plugin not found" en ajoutant `.npmrc` avec `shamefully-hoist=true`
- ✅ Supprimer le job `docker` inutile (pas de Dockerfile)
- ✅ Corriger le warning Rust "unused import: Serialize" dans `routes.rs`

## Étape suivante
- Commit et push pour valider que la CI passe

## Suppositions faites
- Version pnpm : 9 (dernière stable)
- Modifier tous les jobs concernés : react-tests, android, tauri-linux, tauri-windows ✅
- Dépendances GTK nécessaires pour rust-tests
- Projet n'est pas un workspace pnpm → suppression de pnpm-workspace.yaml
- pnpm ne hoiste pas par défaut → ajout de shamefully-hoist=true pour React Native
